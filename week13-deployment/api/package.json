{
  "name": "week13-api",
  "version": "1.0.0",
  "description": "CPAN 212 Week 13: Campus Events API ready to deploy on Render",
  "type": "module",
  "private": true,
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "dev": "node --watch --env-file-if-exists=.env src/server.js",
    "start": "node --env-file-if-exists=.env src/server.js",
    "seed": "node --env-file-if-exists=.env scripts/seed.js",
    "generate-secret": "node scripts/generate-secret.js"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "connect-mongo": "^6.0.0",
    "cors": "^2.8.6",
    "ejs": "^6.0.1",
    "express": "^5.2.1",
    "express-rate-limit": "^8.7.0",
    "express-session": "^1.19.0",
    "helmet": "^8.3.0",
    "mongodb": "^7.6.0",
    "mongoose": "^9.10.0",
    "multer": "^2.3.0"
  },
  "devDependencies": {
    "jose": "^6.2.12"
  }
}
