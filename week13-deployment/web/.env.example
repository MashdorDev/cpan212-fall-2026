# Where the Express API runs. Used by Server Components and by the rewrites in next.config.mjs.
# On Vercel, set it to your Render URL (for example https://campus-events-api.onrender.com) before the first build.
API_ORIGIN=http://localhost:4000
