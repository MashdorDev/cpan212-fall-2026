meta {
  name: Log in with a wrong password
  type: http
  seq: 22
}

post {
  url: {{baseUrl}}/api/auth/login
  body: json
  auth: none
}

body:json {
  {
    "email": "ava@example.com",
    "password": "not-the-password"
  }
}

assert {
  res.status: eq 401
}
