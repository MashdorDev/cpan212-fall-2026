meta {
  name: Holiday check on a regular day
  type: http
  seq: 17
}

get {
  url: {{baseUrl}}/api/events/{{eventId}}/holiday-check
  body: none
  auth: none
}

assert {
  res.status: eq 200
  res.body.data.isHoliday: eq false
}
