meta {
  name: Create tool with broken JSON
  type: http
  seq: 8
}

post {
  url: {{baseUrl}}/api/tools
  body: text
  auth: none
}

headers {
  Content-Type: application/json
}

body:text {
  { "name": "Rake",
}

assert {
  res.status: eq 400
}
