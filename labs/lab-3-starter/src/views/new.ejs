<%# TODO (you): the "Report a lost pet" page.
    Include the header and footer partials, then build a form with
    method="post", action="/notices" and enctype="multipart/form-data".
    Inputs and names are in requirement 6 of the lab page. Put the file input last.
    Wrap each label and input in <div class="field">, show a field's error in
    <p class="field-error">, and give the form class="notice-form".
    Refill each input from values, and select the species the user picked. %>
