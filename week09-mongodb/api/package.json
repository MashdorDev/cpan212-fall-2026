{
  "name": "week09-api",
  "version": "1.0.0",
  "description": "CPAN 212 Week 9: Campus Events API stored in MongoDB with Mongoose",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "dev": "node --watch --env-file-if-exists=.env src/server.js",
    "start": "node --env-file-if-exists=.env src/server.js",
    "seed": "node --env-file-if-exists=.env scripts/seed.js"
  },
  "dependencies": {
    "ejs": "^6.0.1",
    "express": "^5.2.1",
    "mongodb": "^7.6.0",
    "mongoose": "^9.10.0",
    "multer": "^2.3.0"
  }
}
