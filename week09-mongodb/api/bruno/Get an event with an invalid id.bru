meta {
  name: Get an event with an invalid id
  type: http
  seq: 8
}

get {
  url: {{baseUrl}}/api/events/no-such-id
  body: none
  auth: none
}

assert {
  res.status: eq 404
}

docs {
  "no-such-id" can never be an ObjectId, so the API answers 404 without asking the database.
}
